SUBDIRS = src

ACLOCAL_AMFLAGS=-I m4

# Install the pkg-config file.
pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = libkamoflage.pc

DISTCLEANFILES = $(pkgconfig_DATA)
EXTRA_DIST = libkamoflage.pc.in
